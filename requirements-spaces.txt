# HF Spaces deployment dependencies — trimmed from requirements.txt.
# Excludes: torch, transformers, anthropic, shap, matplotlib, seaborn,
#           jupyter, pytest, scipy, pyarrow, Levenshtein (not needed at runtime).
#
# When deploying to HF Spaces, push this file as `requirements.txt` in the Space repo.
# scikit-learn and joblib versions are load-critical: surf_condition_model.joblib
# was serialised with scikit-learn==1.8.0.

gradio>=4.44,<5.0
openai==1.6.1
tavily-python==0.7.23
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv==1.0.0
httpx==0.25.2
requests==2.31.0
numpy==2.4.4
structlog==23.2.0
python-dateutil==2.8.2
pytz==2023.3
cachetools==5.3.2
scikit-learn==1.8.0
joblib==1.5.3
pandas==2.2.3
